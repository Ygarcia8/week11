name: tfsec

on:
  push:
    branches:
      - prod
  pull_request:
    branches:
      - prod

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Check for Terraform files
        id: check_tf
        run: |
          count=$(find . -type f -name "*.tf" | wc -l)
          echo "Found $count Terraform files"
          if [ "$count" -gt 0 ]; then
            echo "run_build=true" >> $GITHUB_ENV
          else
            echo "No Terraform files found. Skipping build steps."
            echo "run_build=false" >> $GITHUB_ENV
          fi

      - name: Install tfsec
        if: env.run_build == 'true'
        run: |
          curl -s https://raw.githubusercontent.com/aquasecurity/tfsec/master/scripts/install_linux.sh | bash
          tfsec --version

      - name: Install tflint
        if: env.run_build == 'true'
        run: |
          curl -L https://github.com/terraform-linters/tflint/releases/latest/download/tflint_linux_amd64.zip -o tflint.zip
          unzip tflint.zip -d /usr/local/bin
          tflint --version

      - name: Run tfsec with SARIF output
        if: env.run_build == 'true'
        run: |
          tfsec --format sarif --out results.sarif --soft-fail .

      - name: Upload SARIF results
        if: env.run_build == 'true'
        uses: github/codeql-action/upload-sarif@v2
        with:
          sarif_file: results.sarif

      - name: Run tflint
        if: env.run_build == 'true'
        run: tflint --chdir .

      - name: Final check
        run: echo "✅ CI steps completed!"


